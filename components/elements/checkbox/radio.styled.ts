import styled from "styled-components";

const Label = styled.label`
  //
`;

const Checkbox = styled.input.attrs({ type: "checkbox" })`
  //
`;

const Span = styled.span`
  position: relative;
  bottom: 1px;
`;

export { Label, Checkbox, Span };
